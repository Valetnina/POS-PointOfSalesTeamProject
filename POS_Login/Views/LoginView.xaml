<Window x:Class="POS_PointOfSales.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:POS_PointOfSales.Views"
        xmlns:ff="clr-namespace:POS_ViewsLibrary;assembly=POS_ViewsLibrary"
         mc:Ignorable="d"
        Title="LoginView" Height="300" Width="300"
        WindowStartupLocation="CenterScreen"
>

    <Grid>
        <TextBox Text="{Binding Path=UserName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors = True}" HorizontalAlignment="Left" Height="23" Margin="91,53,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <PasswordBox x:Name="PasswordBox"  ff:PasswordBoxAssistant.BindPassword="true" ff:PasswordBoxAssistant.BoundPassword="{Binding Path=Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Left" Height="23" Margin="91,93,0,0"  VerticalAlignment="Top" Width="120"/>
        <Button Content="Login" Command="{Binding Login}" xmlns:cmd="#unknown" HorizontalAlignment="Left" Margin="130,154,0,0" VerticalAlignment="Top" Width="75"/>
    </Grid>
</Window>
