<UserControl x:Class="POS_SellersApp.Views.ProductsCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_SellersApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}" x:Key="TextBlockStyle">
                <Setter Property="FontFamily" Value="Comic Sans MS"/>
                <Setter Property="FontSize" Value="25"/>
            </Style>
            <Style TargetType="{x:Type Button}" x:Key="ButtonStyle">
                <Setter Property="Foreground" Value="Aqua"/>
            </Style>
        </Grid.Resources>
        <TextBlock Style="{StaticResource TextBlockStyle}" Text = "Here will be the Products Catalog"/>
        <WrapPanel Orientation="Vertical" Margin="119,21,10,211">
        <ItemsControl ItemsSource="{Binding catalogCollection}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Name}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
           
        </WrapPanel>
    </Grid>
</UserControl>
